const {
  uploadUserPhoto,
  uploadPortfolioImages,
  resizeUserPhoto,
  resizePortfolioImages,
} = require("../config/upload");

module.exports = {
  uploadUserPhoto,
  resizeUserPhoto,
  uploadPortfolioImages,
  resizePortfolioImages,
};
